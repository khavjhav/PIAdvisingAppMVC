@model IEnumerable<PIAdvisingApp.ViewModels.PiAdvisingBondMainViewModel>

@section Css {
    <!-- Add your custom CSS styles here -->
}

<div class="app-page-title">
    <div class="page-title-wrapper">
        <div class="page-title-heading">
            <div>
                Test
            </div>
        </div>
    </div>
</div>

@section Modal {
    <!-- Add your modal code here if needed -->
}

@section Scripts {
    <!-- Add your script references and code here if needed -->
}

<!-- Form for inputting parameters -->
<form method="post" action="@Url.Action("GetPamModalBondPartial", "Sales")">

    <div class="form-group">
        <label for="customerName">Customer Name:</label>
        <input type="text" class="form-control" id="customerName" name="customerName" placeholder="Enter Customer Name">
    </div>
    <div class="form-group">
        <label for="retailerName">Retailer Name:</label>
        <input type="text" class="form-control" id="retailerName" name="retailerName" placeholder="Enter Retailer Name">
    </div>
    <div class="form-group">
        <label for="categoryName">Category Name:</label>
        <input type="text" class="form-control" id="categoryName" name="categoryName" placeholder="Enter Category Name">
    </div>
    <button type="submit" class="btn btn-primary">Search</button>
</form>

<!-- Master view for displaying results -->
<div class="master-view">
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>Booking Number</th>
                <th>Customer Name</th>
                <th>Retailer Name</th>
                <th>Total Price</th>
                <th>Total Quantity</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.BookingNo</td>
                    <td>@item.CustomerName</td>
                    <td>@item.RetailerName</td>
                    <td>@item.InvoiceValue</td>
                    <td>@item.InvoiceQty</td>
                </tr>
                <tr class="detail-row">
                    <td colspan="5">
                        <!-- Detail view for expanded row -->
                        <div class="detail-view">
                            <p>Product Name: @item.ProductName</p>
                            <p>Measurement: @item.Measurement</p>
                            <p>Other details...</p>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

<style>
    .detail-row {
        display: none;
    }

        .detail-row.show {
            display: table-row;
        }
</style>

<script>
    $(document).ready(function () {
        $('.master-view').on('click', 'tbody tr', function () {
            $(this).next('.detail-row').toggleClass('show');
        });
    });
</script>
